import React from 'react'


const Select = (props) => {
    return(
        <>
            <select name="regions">
            <option>Filter by Region</option>
            </select>
        </>
    );
}

export default Select;